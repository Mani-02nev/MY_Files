ls
ls # list all items
ls -l # lang details 
ls -a # show hidden fill 
ls -d */ # show only directories
ls -lh # human read size
ls -s # shorted by size
ls -t # shorted by time
ls -r # rivers order
ls -R # Recursive file 
ls -F # type of fills / directories and * ex fill
ls -u # last access time 
ls -U # un shorted list
# change directory command
cd # same for cd ~ and going for home directory 
cd mani # going to a mani directory
cd . # current directory
cd .. # parent directory
# cd mani/Bash script # going to particular place
cd mani && ls # use in ls to cd 
# tree commend
# tree # same also ls commend but show a tree formate
# tree -L [ numbers(1,2,3,,) ] # step of trees
# tree -a # show a hidden fills
# tree -d # show a dir
# tree -f #  show full path of fille 